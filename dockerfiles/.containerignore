# Dev-only files excluded from the dockerfiles/ build context (base/tools/
# nestable layers). Must mirror dockerfiles/streamd/.containerignore and
# dockerfiles/acpd/.containerignore, which baseImageHash() reads through
# contextHash() — keep them in sync (literal paths only).
streamd/test
streamd/node_modules
acpd/test
acpd/node_modules
