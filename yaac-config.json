{
  "nestedContainers": true,
  "initCommands": [
    "pnpm install",
    "Xvfb :0 -screen 0 1920x1080x24 +extension RANDR -ac >/tmp/xvfb.log 2>&1 & sleep 1 && x11vnc -display :0 -nopw -forever -shared -rfbport 5900 -xrandr resize -bg -o /tmp/x11vnc.log && websockify -D --web=/usr/share/novnc 6080 localhost:5900"
  ],
  "hideInitPane": true,
  "portForward": [
    { "containerPort": 6080, "hostPortStart": 6080 }
  ]
}
